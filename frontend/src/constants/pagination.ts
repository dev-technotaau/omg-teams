// ──────────────────────────────────────────────
//  Pagination Defaults
// ──────────────────────────────────────────────

export const DEFAULT_PAGE = 1;
export const DEFAULT_PAGE_SIZE = 20;
export const DEFAULT_LARGE_PAGE_SIZE = 25;
export const DEFAULT_SMALL_PAGE_SIZE = 10;
export const NOTIFICATION_PAGE_SIZE = 20;
export const NOTIFICATION_DROPDOWN_LIMIT = 20; // §11.2.1 — shows last 20-30 notifications

export const PAGE_SIZE_OPTIONS = [10, 20, 25, 50, 100] as const;
